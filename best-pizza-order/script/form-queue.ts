function getRandomInt(min: number, max: number): number {
    min = Math.ceil(min);
    max = Math.floor(max);
    return Math.floor(Math.random() * (max - min + 1)) + min;
  }

function formQueue() {
    
    rowName = section + rowNumber
    const buildQueue[]
    buildQueue.push (getRandomInt(1,))
}